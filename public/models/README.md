Place GLB/GLTF model files here and reference them from `src/data/initialCars.js` using `model3d: '/models/your-model.glb'`.

Tips:
- Use binary `.glb` for smaller single-file models.
- Ensure models are publicly accessible (no auth) when hosted externally.
- You can also paste a public URL into the Car detail > "GLB/GLTF model URL" input — it saves locally in your browser.

Example filenames to match current placeholders:
- 01-mustang.glb
- 02-camaro.glb
- 03-charger.glb
- 04-impala.glb
- 05-roadrunner.glb
- 06-boss302.glb

If you want, I can add low-poly placeholder GLBs — tell me and I'll include small sample models.